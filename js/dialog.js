'use strict';

(function () {
  var userDialog = window.setup.userDialog;
  console.log(userDialog);

  userDialog.addEventListener('mousedown', function () {
    console.log(userDialog);
  });

  userDialog.addEventListener('mousemove', function () {

  });

  userDialog.addEventListener('mouseup', function () {

  });

})();
